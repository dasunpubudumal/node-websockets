const METRIC = "metric";
const HEARTBEAT = "heartbeat";
const HEARTBEATACK = "heartbeatack";
const HEARTBEATMESSAGE = {"messageType": HEARTBEAT};
const HEARTBEATACKMESSAGE = {"messageType": HEARTBEATACK};

module.exports = {
    METRIC,
    HEARTBEAT,
    HEARTBEATACK,
    HEARTBEATACKMESSAGE,
    HEARTBEATMESSAGE
};